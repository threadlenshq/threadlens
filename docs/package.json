{
  "name": "@threadlens/docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 4750",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro preview --host 0.0.0.0 --port 4751",
    "deploy": "pnpm run build && wrangler pages deploy dist --project-name threadlens-docs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.39.1",
    "astro": "^6.3.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3",
    "wrangler": "^4.90.0"
  }
}
